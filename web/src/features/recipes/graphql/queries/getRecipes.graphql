query GetRecipes($input: PaginationInput) {
    recipes(pagination: $input) {
        edges {
            node {
                id
                currentVersion {
                    name
                }
            }
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}