query GetRecipeVersion($id: ID!, $version: Int!) {
    recipe(id: $id) {
        id
        author {
            id
            username
        }
        version(version: $version) {
            ...RecipeVersionFields
        }
    }
}