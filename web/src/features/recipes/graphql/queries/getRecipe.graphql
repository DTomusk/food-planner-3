query GetRecipe($id: ID!) {
    recipe(id: $id) {
        name
        id
        user {
            id
        }
        ingredientUsages {
            quantity
            unit {
                name
            }
            ingredient {
                name
            }
        }
    }
}