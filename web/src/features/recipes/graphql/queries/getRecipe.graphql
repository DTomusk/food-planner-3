query GetRecipe($id: ID!) {
    recipe(id: $id) {
        name
        id
        user {
            id
        }
        ingredientUsages {
            quantity
            unit {
                symbol
            }
            ingredient {
                name
            }
        }
        prepMins
        cookMins
        portions
    }
}