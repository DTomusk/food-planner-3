query GetRecipe($id: ID!) {
    recipe(id: $id) {
        id
        author {
            id
            username
        }
        currentVersion {
            ...RecipeVersionFields
        }
        draftVersion {
            ...RecipeVersionFields
        }
    }
}