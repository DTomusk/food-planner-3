fragment RecipeConnectionListFields on RecipeConnection {
  edges {
    node {
      id
      createdAt
      currentVersion {
        name
        imgSrc
        description
        animalProductLevel
      }
      author {
        id
        username
      }
    }
  }
  pageInfo {
    hasNextPage
    endCursor
  }
}
