fragment RecipeConnectionListFields on RecipeConnection {
  edges {
    node {
      id
      createdAt
      currentVersion {
        name
        imgSrc
        description
      }
    }
  }
  pageInfo {
    hasNextPage
    endCursor
  }
}
