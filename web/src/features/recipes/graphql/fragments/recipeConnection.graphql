fragment RecipeConnectionListFields on RecipeConnection {
  edges {
    node {
      id
      currentVersion {
        name
        imgSrc
      }
    }
  }
  pageInfo {
    hasNextPage
    endCursor
  }
}
