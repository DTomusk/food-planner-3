type Recipe {
  id: ID!
  name: String!
}

extend type Query {
  recipes: [Recipe!]!
  recipe(id: ID!): Recipe
}

input NewRecipe {
  name: String!
}

extend type Mutation {
  createRecipe(input: NewRecipe!): Recipe!
}
