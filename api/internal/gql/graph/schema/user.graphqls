type User {
    id: ID!
    email: String!
    username: String!
    recipes(
        pagination: PaginationInput
        filter: RecipeFilterInput
    ): RecipeConnection! @goField(forceResolver: true)
}

extend type Query {
    user(id: ID!): User
    me: User @auth
}